<h1>Editing Event Host</h1>

<%= render 'form', event_host: @event_host %>

<%= link_to 'Show', [@event,@event_host], :class => 'btn btn-info' %>
<%= link_to 'Back', event_event_hosts_path, :class => 'btn btn-success' %>
